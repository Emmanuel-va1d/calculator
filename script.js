const add = (x, y) => x + y;
const subtract = (x, y) => x - y;
const multiply = (x, y) => x * y;
const divide = (x, y) => x / y;

let num1;
let num2;
let operator;

const operate = (x, y, op) => op(x, y);
